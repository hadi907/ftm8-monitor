name: 💧 Water Reminder - هادي

on:
  schedule:
    # الكويت UTC+3 — كل ساعتين
    # السبت(6) الأحد(0) الثلاثاء(2) الأربعاء(3) الجمعة(5) — 7ص إلى 11م
    - cron: '0 4  * * 0,2,3,5,6'   # 7ص  الكويت
    - cron: '0 6  * * 0,2,3,5,6'   # 9ص  الكويت
    - cron: '0 8  * * 0,2,3,5,6'   # 11ص الكويت
    - cron: '0 10 * * 0,2,3,5,6'   # 1م  الكويت
    - cron: '0 12 * * 0,2,3,5,6'   # 3م  الكويت
    - cron: '0 14 * * 0,2,3,5,6'   # 5م  الكويت
    - cron: '0 16 * * 0,2,3,5,6'   # 7م  الكويت
    - cron: '0 18 * * 0,2,3,5,6'   # 9م  الكويت
    - cron: '0 20 * * 0,2,3,5,6'   # 11م الكويت
    # الاثنين(1) الخميس(4) — 6م إلى 10م
    - cron: '0 15 * * 1,4'          # 6م  الكويت
    - cron: '0 17 * * 1,4'          # 8م  الكويت
    - cron: '0 19 * * 1,4'          # 10م الكويت
  workflow_dispatch:

jobs:
  send-water-reminder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Send Water Reminder 💧
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        run: python water_reminder.py
